node_modules/
npm-debug.log*

# Dados persistentes locais (usados fora do Docker, ex.: testes)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
uploads/final/*
uploads/quarantine/*
!uploads/final/.gitkeep
!uploads/quarantine/.gitkeep

# Ambiente
.env
.env.local

# Editores / SO
.vscode/
.idea/
.DS_Store
*.log

# Binários compilados da CLI (cli/filepad-clean*)
cli/dist/
cli/filepad-clean
cli/filepad-clean.exe

# Binários compilados do servidor standalone
standalone/dist/
standalone/filepad-server
standalone/filepad-server.exe
standalone/standalone

# Binários do cloudflared descarregados por fetch-cloudflared.sh (grandes,
# não fazem sentido no repositório — go:embed só precisa deles em tempo de
# compilação local)
standalone/assets/

# App desktop: dependências e builds do Electron
desktop/node_modules/
desktop/dist/
