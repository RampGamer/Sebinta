node_modules/
npm-debug.log*

# Local persistent data (used outside Docker, e.g. tests)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
uploads/final/*
uploads/quarantine/*
!uploads/final/.gitkeep
!uploads/quarantine/.gitkeep

# Environment
.env
.env.local

# Editors / OS
.vscode/
.idea/
.DS_Store
*.log

# Compiled CLI binaries (cli/sebinta-clean*)
cli/dist/
cli/sebinta-clean
cli/sebinta-clean.exe

# Compiled standalone server binaries
standalone/dist/
standalone/sebinta-server
standalone/sebinta-server.exe
standalone/standalone

# cloudflared binaries downloaded by fetch-cloudflared.sh (large, don't
# belong in the repo — go:embed only needs them at local build time)
standalone/assets/

# Desktop app: Electron dependencies and builds
desktop/node_modules/
desktop/dist/
